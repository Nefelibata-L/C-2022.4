#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
//求最大值
int Max(int a, int b) {
	if (a > b)
		return a;
	else 
		return b;
}

int main() {
	int x=0;
	int y=0;
	int m=0;
	scanf("%d %d", &x, &y);
	m=Max(x,y);
	printf("%d\n", m);
}
*/

/*
//判断是否被五整除，输出Yes|No

int main() {
	int i = 0;
	scanf("%d", &i);
	if (i % 5 == 0) {
		printf("%s\n", "Yes\0");
	}
	else
		printf("%s\n", "No\0");
}
*/


//分支语句
